[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "homage-neuro-efficiency"
version = "0.1.0"
description = "Research-backed neuro-efficiency planning layer for ATANOR."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.setuptools]
packages = ["neuro_efficiency"]
