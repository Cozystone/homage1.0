# Turbovec Sandbox

Turbovec Sandbox is a proof-only vector compression layer. It demonstrates
deterministic int8 quantization, a hot/cold storage split estimate, and recall
metric calculation on fixtures.

It does not replace or mutate any production vector store.
