# AirLLM Offload Sandbox

AirLLM Offload Sandbox is an advisory-only planner. It estimates model weight
size, layer split, disk budget, RAM budget, and VRAM budget without downloading
or executing any model.

It is not integrated into the default answer path.
