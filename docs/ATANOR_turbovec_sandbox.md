# ATANOR Turbovec Sandbox

Status: proof-only compression sandbox.

The Turbovec sandbox tests deterministic int8 quantization, fixture recall
measurement, and a hot/cold vector split budget. It is advisory only. It does
not replace production vector storage, mutate Cloud Brain, write Local Brain,
or change any default retrieval path.
